/**
 * Stub for future Python Prophet ML bridge.
 * Will spawn a Python subprocess to run forecasting models
 * and return predictions for volume/price patterns.
 */
export async function forecast(ticker, candles) {
  // TODO: spawn python worker, pass candles as JSON, return forecast
  return null;
}
